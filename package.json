{
  "name": "craft-spb",
  "private": true,
  "version": "1.0.0",
  "description": "CRAFT barbershop — Saint Petersburg. Static site.",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/compress-images.mjs && vite build",
    "preview": "vite preview",
    "images": "node scripts/compress-images.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^6.0.0"
  }
}
